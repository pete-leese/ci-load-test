apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-echo-bar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: http-echo-bar
  template:
    metadata:
      labels:
        app: http-echo-bar
    spec:
      containers:
      - name: http-echo
        image: hashicorp/http-echo
        args:
        - "-text=bar"
        ports:
        - containerPort: 5678
